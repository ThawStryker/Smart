# 调试

本地：http://localhost:7775（`edgespark dev`）
生产：https://leading-stallion-5780.edgespark.app

- Work 正式页：`/work`（DSH 时间线）
- 引擎调试页：`/test/mose-debug.html`（需先登录，打 `POST /api/work/chat`）

本地对话要调模型，在 `.env.local` 填 `DEEPSEEK_API_KEY`（不要提交、不要发给 Agent）。

不要再往仓库根目录丢 `.debug-*`、cookie dump、snapshot 脚本。

不要再往仓库根目录丢 `.debug-*`、cookie dump、snapshot 脚本。
